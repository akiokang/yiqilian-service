server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: yiqilian-biz-auth
  
  # 临时关闭数据库连接检查（因为还没配置真实的 MySQL）
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
