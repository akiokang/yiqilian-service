# 视频上传接口文档

## 接口信息

### 接口路径
`POST /admin/video/upload`

### 接口功能
上传视频文件和视频信息，支持同时上传视频封面图，文件临时存储在本地F盘，视频路径信息存储到数据库。

## 请求参数

### 请求方式
`POST`，`Content-Type: multipart/form-data`

### 请求参数说明

| 参数名 | 类型 | 是否必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `file` | `file` | 是 | 视频文件 |
| `coverFile` | `file` | 否 | 视频封面图文件 |
| `subCategoryId` | `long` | 是 | 细分部位ID |
| `title` | `string` | 是 | 视频标题 |
| `description` | `string` | 是 | 视频详细描述/动作要领 |
| `author` | `string` | 是 | 视频作者/教练名称 |
| `level` | `string` | 是 | 难度等级 (初级/中级/高级) |

### 请求示例

```http
POST /admin/video/upload HTTP/1.1
Host: localhost:8081
Content-Type: multipart/form-data; boundary=----WebKitFormBoundary7MA4YWxkTrZu0gW

------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="file"; filename="example.mp4"
Content-Type: video/mp4

[视频文件二进制数据]
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="coverFile"; filename="cover.jpg"
Content-Type: image/jpeg

[封面图二进制数据]
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="subCategoryId"

1
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="title"

眼部训练视频
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="description"

这是一个眼部训练视频，帮助缓解眼部疲劳
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="author"

张教练
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="level"

初级
------WebKitFormBoundary7MA4YWxkTrZu0gW--
```

## 响应参数

### 响应格式
`application/json`

### 成功响应

| 参数名 | 类型 | 描述 |
| :--- | :--- | :--- |
| `code` | `string` | 响应码，成功为 "0000" |
| `message` | `string` | 响应消息，成功为 "视频上传成功" |
| `data` | `object` | 视频信息对象 |
| `data.id` | `long` | 视频ID |
| `data.subCategoryId` | `long` | 细分部位ID |
| `data.title` | `string` | 视频标题 |
| `data.description` | `string` | 视频描述 |
| `data.author` | `string` | 视频作者 |
| `data.coverUrl` | `string` | 封面图路径 |
| `data.videoUrl` | `string` | 视频路径 |
| `data.level` | `string` | 难度等级 |
| `data.createTime` | `string` | 创建时间 |
| `data.publishTime` | `string` | 发布时间 |

### 失败响应

| 参数名 | 类型 | 描述 |
| :--- | :--- | :--- |
| `code` | `string` | 响应码，失败为非 "0000" |
| `message` | `string` | 错误消息 |
| `data` | `null` | 数据为null |

### 响应示例

#### 成功响应

```json
{
  "code": "0000",
  "message": "视频上传成功",
  "data": {
    "id": 1,
    "subCategoryId": 1,
    "title": "眼部训练视频",
    "description": "这是一个眼部训练视频，帮助缓解眼部疲劳",
    "author": "张教练",
    "coverUrl": "covers/a1b2c3d4e5f6.jpg",
    "videoUrl": "videos/a1b2c3d4e5f6.mp4",
    "originUrl": null,
    "level": "初级",
    "publishTime": "2026-02-09 19:30:00",
    "createTime": "2026-02-09 19:30:00",
    "updateTime": null,
    "isDeleted": 0
  }
}
```

#### 失败响应

```json
{
  "code": "9999",
  "message": "视频上传失败：文件大小超过限制",
  "data": null
}
```

## 存储策略

### 本地存储
- 视频文件存储路径：`F:\yiqilian-videos\videos\`
- 封面图存储路径：`F:\yiqilian-videos\covers\`
- 文件命名规则：使用UUID生成唯一文件名，保留原始文件扩展名

### 后续扩展

#### 切换到阿里云OSS
1. 在 `AdminVideoServiceImpl` 中添加阿里云OSS配置
2. 修改 `uploadFileToLocal` 方法，添加OSS上传逻辑
3. 根据配置选择存储方式

#### 切换到服务器存储
1. 修改 `BASE_STORAGE_PATH` 为服务器存储路径
2. 确保服务器目录存在且有写入权限

## 注意事项

1. 视频文件大小限制：默认500MB（可在application.yml中修改）
2. 支持的视频格式：取决于前端选择的文件类型
3. 封面图建议使用JPG或PNG格式
4. 细分部位ID必须是已存在的ID
5. 难度等级建议使用：初级、中级、高级

## 代码结构

### Controller层
- `AdminVideoController.java`：处理视频上传请求

### Service层
- `AdminVideoService.java`：视频上传服务接口
- `AdminVideoServiceImpl.java`：视频上传服务实现

### 存储逻辑
- 本地存储：`F:\yiqilian-videos\`
- 数据库存储：`yql_video`表
